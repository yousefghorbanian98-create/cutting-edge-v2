[pytest]
minversion = 7.0
testpaths = tests
markers =
    unit: fast, pure-import/API tests
    real: live-server / real-media tests (S-002 backend boot and later)
    e2e: end-to-end UI journeys
    perf: performance/budget tests
    chaos: self-healing chaos probes
