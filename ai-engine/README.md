# Cutting Edge AI Engine

Cutting Edge v2 — AI core backend (FastAPI + MediaPipe + Whisper).

```bash
pip install -r requirements.txt
uvicorn ai_engine.main:app --port 8001
```

پک‌بک‌اند هوش مصنوعیِ «Cutting Edge v2» — اجرا به‌صورت پکیج `ai_engine`.
