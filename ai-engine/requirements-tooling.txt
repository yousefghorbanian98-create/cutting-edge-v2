# Tooling + test dependencies (S-008/S-009). Runtime deps live in requirements.txt.
# Installed into ai-engine/.venv by CI (ubuntu: this file only + web deps below;
# windows: requirements.txt + this file). Exact pins, no ranges.
ruff==0.8.4
bandit==1.8.0
pip-audit==2.7.3
pytest==9.1.1
PyYAML==6.0.3

# Minimal web stack so the ubuntu job can boot the API for unit-level probes
# without the 1.5 GB media/AI wheels.
fastapi==0.141.1
starlette==1.3.1
uvicorn[standard]==0.32.1
python-multipart==0.0.32
pydantic==2.10.4
python-dotenv==1.2.3
psutil==6.1.0
requests==2.34.2
numpy==1.26.4
