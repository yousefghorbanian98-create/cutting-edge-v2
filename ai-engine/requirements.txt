# Cutting Edge v2 — AI core backend dependencies (pinned, see S-002).
# The lock source of truth for the runtime is pyproject.toml; this file mirrors
# it so `pip install -r requirements.txt` produces the same environment.

# Web / API
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.18
pydantic==2.10.4
python-dotenv==1.0.1
requests==2.32.3
psutil==6.1.0

# AI / media processing
numpy==1.26.4
scipy==1.11.4
opencv-python==4.10.0.84
mediapipe==0.10.14
faster-whisper==1.1.0
scenedetect[opencv]==0.6.4
moviepy==2.1.1
librosa==0.10.2.post1
imageio-ffmpeg==0.5.1
gputil==1.4.0
edge-tts==6.1.12
