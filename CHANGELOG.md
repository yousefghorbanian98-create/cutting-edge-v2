# Changelog

## 0.3.0 — not released

Timeline zoom, sequence preview, shortcut registry, and the v0.3 journey are implemented on the builder branch. This version is not tagged and has no GitHub pre-release.

`verified_on` does not include `user-gpu`. A dry-run of `scripts/smoke-gpu.ps1` is not a machine pass. The user still has to run the installer on Windows/GTX 1650 and record `app_launch`, `backend_health`, and `import_ok`.

S-023 through S-032 stay `REVIEW`. None of them are `GREEN`. S-033 has not started.
